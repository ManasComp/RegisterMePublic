@using Microsoft.AspNetCore.Mvc.TagHelpers
@model WebGui.Areas.Visitor.Models.OrganizationModel

<nav aria-label="Page navigation">
    <ul class="pagination pagination-sm n justify-content-center">
        @if (Model.Organizations.HasPreviousPage)
        {
            <li class="page-item">
                <a class="page-link" asp-action="Index"
                   asp-route-searchString="@Model.OrganizationsFilterDto.SearchString"
                   asp-route-OrganizationConfirmationStatus="@Model.OrganizationsFilterDto.OrganizationConfirmationStatus"
                   asp-route-pageNumber="@(Model.Organizations.PageNumber - 1)" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>
        }

        @for (var i = 1; i <= Model.Organizations.TotalPages; i++)
        {
            <li class="page-item @(i == Model.Organizations.PageNumber ? "active" : "")">

                @{
                    var i1 = i;
                }
                <a class="page-link" asp-action="Index"
                   asp-route-searchString="@Model.OrganizationsFilterDto.SearchString"
                   asp-route-OrganizationConfirmationStatus="@Model.OrganizationsFilterDto.OrganizationConfirmationStatus"
                   asp-route-pageNumber="@i1">@i1</a>
            </li>
        }

        @if (Model.Organizations.HasNextPage)
        {
            <li class="page-item">
                <a class="page-link" asp-action="Index"
                   asp-route-searchString="@Model.OrganizationsFilterDto.SearchString"
                   asp-route-OrganizationConfirmationStatus="@Model.OrganizationsFilterDto.OrganizationConfirmationStatus"
                   asp-route-pageNumber="@(Model.Organizations.PageNumber + 1)" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
        }
    </ul>
</nav>